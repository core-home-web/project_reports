{
  "repos": [
    "owner/repo-name-1",
    "owner/repo-name-2",
    "another-owner/another-repo"
  ],
  "description": "Example repository configuration. In the multi-tenant version, users select their own repos via the UI, and this data is stored per-user in Cloudflare KV.",
  "notes": [
    "Each repo should be in the format: owner/repository-name",
    "Users can select any repos they have access to on GitHub",
    "The selection is saved to KV with key: user:{userId}:repos"
  ]
}
