# GitHub OAuth Configuration
# Get these from: https://github.com/settings/developers
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id_here
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret_here

# OAuth Callback URL
# This should match the callback URL registered in your GitHub OAuth app
# Format: https://your-worker-name.your-subdomain.workers.dev/auth/github/callback
OAUTH_REDIRECT_URI=https://your-worker.workers.dev/auth/github/callback

# Session Security
# Generate a random 32+ character string for session encryption
# Example: openssl rand -hex 32
SESSION_SECRET=your_random_session_secret_minimum_32_characters

# Token Encryption
# Generate another random 32+ character string for encrypting GitHub tokens
# Example: openssl rand -hex 32
ENCRYPTION_KEY=your_random_encryption_key_minimum_32_characters

# Frontend URL
# The URL where your frontend is hosted (for CORS and redirects)
# Local dev: http://localhost:8080
# Production: https://your-domain.vercel.app
FRONTEND_URL=http://localhost:8080

# Optional: GitHub Personal Access Token (for testing without OAuth)
# Only needed during development if you want to test API calls without OAuth
# GITHUB_TOKEN=ghp_your_personal_access_token_here

# Cloudflare Configuration (set in Cloudflare dashboard, not here)
# These are documented here for reference only:
# - KV Namespace ID (set in wrangler.toml)
# - Account ID (set in wrangler.toml)

# ========================================
# SETUP INSTRUCTIONS
# ========================================
#
# 1. Copy this file to .env (for local development)
#    cp .env.example .env
#
# 2. For Cloudflare Workers, use .dev.vars (ignored by git):
#    cp .env.example .dev.vars
#
# 3. Fill in your actual values
#
# 4. For production, set secrets using Wrangler CLI:
#    wrangler secret put GITHUB_OAUTH_CLIENT_ID
#    wrangler secret put GITHUB_OAUTH_CLIENT_SECRET
#    wrangler secret put SESSION_SECRET
#    wrangler secret put ENCRYPTION_KEY
#    wrangler secret put FRONTEND_URL
#
# 5. NEVER commit .env or .dev.vars to version control!
#
# ========================================
# SECURITY NOTES
# ========================================
#
# - Keep SESSION_SECRET and ENCRYPTION_KEY private
# - Use different keys for development and production
# - Rotate keys periodically (every 90 days recommended)
# - Never hardcode secrets in your code
# - Use environment variables in production
#
